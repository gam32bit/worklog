[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "worklog"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["pyyaml"]

[project.scripts]
log = "log:main"

[tool.setuptools]
py-modules = ["log"]
packages = ["worklog"]
